@model IEnumerable<Candidate>

@if (Model.Count() > 0)
{

}
else
{
    <div class="alert alert-info">
        There is no aplications.Please click here to
        <a class="alert-link" asp-action="Apply">apply</a>
    </div>
}